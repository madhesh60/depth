# Training + ONNX export — Kaggle GPU only. NOT installed on the live server
# (the runtime path is torch-free; it infers via cv2.dnn — see requirements.txt).
ultralytics==8.4.157
torch==2.9.1
torchvision==0.24.1
onnxruntime==1.23.2
PyYAML==6.0.2
Pillow==11.1.0
# onnx + onnxslim are pulled automatically by `yolo export format=onnx`.
