# Runtime — the live FastAPI app + OpenCV-5 inference (cv2.dnn, torch-free).
# This is what runs on the Graviton + COOL demo server; keep it small.
# Pinned EXACTLY to the versions the demo is tested on (competition rule: OpenCV 5
# must do real work — see docs/WINNING_REPORT.md §5.1).
#   Training / ONNX export deps -> requirements-train.txt (Kaggle GPU only)
#   Test-suite deps             -> requirements-dev.txt
opencv-python-headless==5.0.0.93
numpy==2.2.6            # OpenCV 5's cp310 wheel requires numpy>=2
fastapi==0.127.0
uvicorn==0.40.0
python-multipart==0.0.21
boto3==1.43.98
mcp==1.28.1             # Model Context Protocol server (stdio + Streamable HTTP at /mcp)
