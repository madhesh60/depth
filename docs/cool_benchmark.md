# COOL benchmark — the DEPTH product workload, per stage

_Generated by `python -m src.bench.compare` from `runs/bench/*.json` (`python -m src.bench.product_bench`)._

Workload `product` = exactly what `/api/analyze` runs per frame: JPEG decode → Stage 1 (canonicalise + bottom track) → YOLO11 via `cv2.dnn` → evidence + guaranteed tiers → overlay + JPEG encode. `cv` = the same minus the network (the OpenCV image-processing share, incl. `cv2.remap` and range gain). A run counts as **COOL** only if `cv2.__file__` is under `/opt/cool` (provenance, not "KleidiCV in the build", which the stock Arm wheel also reports).

| run | workload | host | OpenCV | threads | total ms p50 / p95 | per stage p50 (ms) | fps | $ / 1k frames | survey-hour: compute · real-time factor · $ |
|---|---|---|---|--:|--:|---|--:|--:|---|
| `laptop_x86` | cv | AMD64 · Intel64 Family 6 Model 140 S | 5.0.0 stock | 8 | **22.2** / 28.5 | decode 3 · stage1 16 · render 3 | 42.03 | — | 3.9 s · RTF 0.0011 · — |
| `laptop_x86` | product | AMD64 · Intel64 Family 6 Model 140 S | 5.0.0 stock | 8 | **238.0** / 397.1 | decode 5 · stage1 5 · see 205 · prove_decide 10 · render 4 | 3.89 | — | 43.2 s · RTF 0.0120 · — |
| `laptop_x86_t1` | product | AMD64 · Intel64 Family 6 Model 140 S | 5.0.0 stock | 1 | **522.5** / 550.8 | decode 3 · stage1 3 · see 505 · prove_decide 6 · render 2 | 1.9 | — | 88.7 s · RTF 0.0246 · — |

_A/B/C comparison appears once the EC2 runs (`infra/bench_cool.sh`) are in `runs/bench/`._

**Survey-hour model:** frames per survey-hour = ping rate × 3600 / pings per frame × channels (assumptions recorded in each JSON; default 15 Hz, 640 pings/frame, port + starboard). Real-time factor = compute seconds per hour of sonar ÷ 3600 (< 1 ⇒ keeps up with the boat).

---
_Numbers in this file are regenerated by the script; do not edit by hand._
