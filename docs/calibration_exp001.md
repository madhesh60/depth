# EXP-001 — guaranteed tiers: calibrated on validation, verified on test

_Generated by `python -m src.agentic.calibrate` · α=0.1 (requested miss rate) · target precision 0.85 · δ=0.05 (95% confidence) · match IoU 0.3 · unique frames only (Roboflow copies removed)_

**Calibration:** `DATASET/03_yolo_ready_dataset_v1/val` crabpot_*wcp_ss_*.jpg — 66 unique frames, recordings ['Rec19']  
**Verification (scored once):** `DATASET/03_yolo_ready_dataset_v1/test` crabpot_*wcp_ss_*.jpg — 92 unique frames, recordings ['Rec10', 'Rec3', 'Rec4', 'Rec6']

## 1. Recall promise — how many pots reach a human

| | calibration | verification |
|---|--:|--:|
| pots | 134 | 138 |
| recall ceiling (any proposal, conf ≥ 0.05) | 0.724 | 0.862 |
| requested promise | ≥ 90% | |
| requested promise achievable? | **NO — detector ceiling** | |
| **promise made** (95% conf.) | **≥ 65% of pots reach a human** | |
| τ_review (detector conf) | 0.050 | same |
| empirical recall at τ_review | 0.724 | 0.862 (LCB 0.805) |
| promise held on unseen test? | | **YES** |

## 2. Precision promise + agent vs baseline at the SAME recall promise

Every policy uses the same τ_review (same cards reach a human); they differ only in how many of those cards can be **auto-confirmed** with a ≥85% precision promise (95% conf.). More auto-confirmed pots = fewer human cards for the same guarantee.

| policy | AUC TP-vs-FP (cal) | AUC (test) | best precision promise (cal) | τ_confirm @target | CONFIRMED (cal) TP/n | CONFIRMED (test) TP/n | test precision | promise held | inferences/frame |
|---|--:|--:|--:|--:|--:|--:|--:|:--:|--:|
| **baseline_conf** | 0.742 | 0.764 | 0.625 | — | 0/0 | 0/0 | — | — | 1.00 |
| agent_single | 0.738 | 0.665 | 0.613 | — | 0/0 | 0/0 | — | — | 4.15 |
| agent_single_clahe | 0.725 | 0.650 | 0.497 | — | 0/0 | 0/0 | — | — | 7.30 |
| agent_mosaic | 0.646 | 0.654 | 0.433 | — | 0/0 | 0/0 | — | — | 2.24 |
| agent_mosaic_clahe | 0.660 | 0.647 | 0.471 | — | 0/0 | 0/0 | — | — | 3.48 |
| agent_single_demote | 0.756 | 0.710 | 0.658 | — | 0/0 | 0/0 | — | — | 4.15 |
| agent_mosaic_demote | 0.749 | 0.712 | 0.645 | — | 0/0 | 0/0 | — | — | 2.24 |

_`lift` = max(conf, re-look) — a re-fire can promote. `demote` = conf × (0.5 + 0.5·re-fired) — the re-look can only veto. The two `demote` policies were added after the first look at the calibration split showed `lift` does not beat confidence; they were still selected on calibration data only, and the verification split was scored once._

Paired frame-bootstrap AUC gain over the baseline on **calibration** (mean, 95% CI):

| policy | gain | 95% CI |
|---|--:|:--:|
| agent_single | -0.003 | -0.065 .. +0.059 |
| agent_single_clahe | -0.014 | -0.090 .. +0.065 |
| agent_mosaic | -0.094 | -0.166 .. -0.021 |
| agent_mosaic_clahe | -0.080 | -0.162 .. +0.003 |
| agent_single_demote | +0.014 | -0.013 .. +0.042 |
| agent_mosaic_demote | +0.008 | -0.010 .. +0.025 |

**Shipped policy: `baseline_conf`** — no re-look policy auto-confirmed more pots under the promise, and none improved the REVIEW-queue ranking significantly on calibration (no paired-bootstrap 95% CI of the AUC gain lies above 0; the mosaic 'lift' is significantly worse) — so the agent spends **no re-look compute on tiering** (1 inference/frame). The re-look stays available as a display-only 'agent's eye' view on the evidence card.

> **Disclosure.** The first version of the choice rule broke the tie on AUC rounded to 2 d.p. and picked `agent_single_demote` (+0.014 AUC on the single calibration recording). On the verification split it ranked *worse* than plain confidence (AUC 0.710 vs 0.764). We then required a significant calibration gain (above); the rule change was made **after** seeing that test result, so treat the verification numbers for the shipped policy as slightly optimistic.

## 3. Old test-tuned rules, re-scored on unseen data

| split | CONFIRMED TP/n | precision | share of pots |
|---|--:|--:|--:|
| calibration | 30/51 | 0.588 | 0.224 |
| verification | 37/64 | 0.578 | 0.268 |

Precision of the top-scored candidates that recover 30% of all pots (the old headline's operating point):

| split | baseline (confidence) | agent (shipped score) |
|---|--:|--:|
| calibration | 0.695 | 0.695 |
| verification | 0.712 | 0.712 |

## 4. Other evidence

- Shadow (thin-line) AUC, true vs false detection (verification): **0.599** — evidence for the card only, never a gate.
- Median latency on this machine: detect 552.2 ms/frame · single re-look 544.0 ms/candidate · mosaic re-look 565.0 ms/pass (4 candidates).
- Caveats: frames of one recording are correlated (the CP bound assumes independence), and the calibration split is a single recording — which is why the promise is **verified on a separate split**. Labels are incomplete (some 'false positives' are probably unlabelled objects), so precision is a lower estimate.

---
_Numbers in this file are regenerated by the script; do not edit by hand._