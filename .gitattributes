# Vendored third-party assets are served with SRI hashes: keep their bytes exact (no EOL conversion).
webui/vendor/** -text
# Shell scripts and systemd units run on Linux: always LF, even in a Windows checkout.
*.sh text eol=lf
*.service text eol=lf
